import fetch from "node-fetch";

export default ({ clientId, orgId, accessToken }) => {

  return {
    createSession: async (sessionName) => {
    },
    fetchEvents: async (sessionId) => {
    }
  };
}
