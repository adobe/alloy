# Include root when running recursive commands (optional but convenient)
include-workspace-root=true

# Helpful defaults
save-workspace-protocol=true
prefer-workspace-packages=true

# Enable automatic peer installs if needed by tooling (optional)
auto-install-peers=true